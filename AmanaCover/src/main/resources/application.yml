spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/amanacover
    username: postgres
    password: Devos#88
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
        open-in-view: false
